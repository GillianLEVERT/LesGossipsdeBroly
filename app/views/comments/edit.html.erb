<div class="page content col-7">
<h2>Tu as écrit de la merde ? tu veux le modifier ? c'est ici :</h2><br>
  <%= form_for [@gossip, @comment] do |form|%>
    <div class="form-group">

    <label> Ton commentaire : </label>
    <%= form.text_field :content, class: "form-control" %>
    <br><br>
    </div>

    <%= submit_tag "MODIFIER", class: 'btn btn-outline-primary btn-sm' %>

  <% end %>
  
</div>